version: 2

models:
  - name: discounts
    columns:
      - name: discount
        config:
          databricks_tags:
            my_key: "dante"
        tests:
          - accepted_values:
              values: ["discounted", "not_discounted"]
